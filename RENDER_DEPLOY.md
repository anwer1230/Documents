# نشر مركز سرعة إنجاز على Render

ملف `render.yaml` يعرّف خدمة الويب ويضبط فحص الصحة والنشر التلقائي من فرع
`main`. بعد ربط المستودع في Render، اختر **New > Blueprint** ثم ارفع هذا
المستودع.

## المتغيرات المطلوبة

أضف القيم التالية في Render Environment قبل أول تشغيل:

- `TELEGRAM_API_ID`
- `TELEGRAM_API_HASH`
- `GROQ_API_KEY`
- `GITHUB_TOKEN`

يتم إنشاء `SESSION_SECRET` تلقائيًا بواسطة `render.yaml`. أما
`VAPID_PUBLIC_KEY` و`VAPID_PRIVATE_B64` فهما اختياريان لإشعارات المتصفح.

## ملاحظة عن خطة Render

الإعداد الحالي يستخدم الخطة المجانية لتجنب تكلفة غير متوقعة. الخطة المجانية
قد توقف خدمة الويب عند الخمول، لذلك لا تناسب المراقبة المستمرة لرسائل Telegram.
للتشغيل الدائم، غيّر `plan` إلى `starter` من إعدادات Render بعد التأكد من
الخطة والتكلفة المناسبة لك.

لا تضع أي قيمة سرية داخل ملفات المشروع أو GitHub؛ استخدم صفحة Environment في
Render.